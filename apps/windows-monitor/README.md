# windows-monitor (public artefacts)

Optional — populated when Windows publish outputs exist in the dev repo.

After `./playbook` builds / `publish-full-installer.ps1`:

- `publish/fullinstall/` — full installer artefacts
- `publish/rollout-agent/` — rollout agent EXE

Source code stays in `easymonitoring`; only published installers are mirrored here.
