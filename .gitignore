# Secrets — never commit
.env
.env.*
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets/
**/secrets/

# Runtime monitor state (local only — never publish)
*auth*.json
synology-auth.json
unix-auth.json
synology-monitor.json
unix-monitor.json
windows-monitor.json
.sync-hint
state/
.state/

# Local signing / packaging keys
*.sig
signing/
codesign/

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Python / Node scratch (artefacts may ship .py install scripts)
__pycache__/
*.pyc
.pytest_cache/
.venv/
node_modules/

# Synology SPK build scratch (committed outputs: dist/*.spk, repo/, package/)
community-package/.build/
**/.build/

# Allowed and expected in this repo:
# - apps/**/deploy/**/*.sh
# - apps/**/community-package/dist/*.spk
# - apps/**/community-package/repo/packages.json
# - apps/windows-monitor/publish/**/*.exe
